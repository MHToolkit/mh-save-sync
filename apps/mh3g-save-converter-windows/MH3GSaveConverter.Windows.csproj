<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <RootNamespace>MHToolkit.MH3GSaveConverter.Windows</RootNamespace>
    <AssemblyName>MH3GSaveConverter</AssemblyName>
    <UseWinUI>true</UseWinUI>
    <EnableMsixTooling>true</EnableMsixTooling>
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Assets\MH3GSaveConverter.ico</ApplicationIcon>
    <Platforms>x64</Platforms>
    <PlatformTarget>x64</PlatformTarget>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PublishReadyToRun>false</PublishReadyToRun>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- 1.8.8+ repairs the Windows App SDK XAML compiler's CI diagnostic path;
         1.8.10 is the current stable servicing release and retains the same
         Windows 10 1809 minimum supported OS as the existing 1.7 shell. -->
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.8.260710003" />
  </ItemGroup>

  <ItemGroup>
    <Content Update="Assets\MH3GSaveConverter.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Update="Assets\Artwork\*.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <!-- Release packaging places the signed Rust CLI here. The UI remains
         usable for path selection and report review until that sidecar exists. -->
    <None Update="tools\mh3g-save-convert.exe">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <!-- The package script temporarily supplies this native sidecar before
           single-file publish, then restores the source tree. Keep it inside
           the bundle so IncludeAllContentForSelfExtract makes tools/ available
           under the single-file app's extracted AppContext.BaseDirectory. -->
      <IncludeInSingleFile>true</IncludeInSingleFile>
    </None>
  </ItemGroup>
</Project>
